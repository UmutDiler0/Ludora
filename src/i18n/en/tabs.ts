/**
 * Tab bar labels. Used to live as a mixed-language constant in
 * `constants/app.ts` (some already Turkish, predating this i18n system) —
 * moved here once the app needed a real English tree, not an accidental one.
 */
export const tabs = {
  home: 'Home',
  leaderboard: 'Leaderboard',
  play: 'Play',
  profile: 'Profile',
};
